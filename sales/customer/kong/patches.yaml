_format_version: "1.0"

patches:
  - selectors:
    - $.services[?(@.name=="customer-information-service")]
    values:
      host: kongair-flights-service.kongair.svc.cluster.local
      port: 8080
      protocol: http
