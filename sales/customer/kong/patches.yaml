_format_version: "1.0"

patches:
  - selectors:
    - $.services[?(@.name=="customer-information-service")]
    values:
      host: kongair-customers-service.kongair.svc.cluster.local
      port: 8083
      protocol: http
